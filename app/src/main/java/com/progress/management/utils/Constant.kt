package com.progress.management.utils

import android.Manifest

internal const val DEVICE = 2
class Constant {
    companion object {
        const val DATE_FORMAT_1 = "yyyy/MM/dd"
        const val DATE_FORMAT_2 = "dd/MM/yyyy"
    }
}

